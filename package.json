{
  "name": "aipal",
  "version": "0.1.4",
  "private": true,
  "description": "Telegram bot that forwards messages to a local CLI agent (Codex by default).",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "node --check src/index.js",
    "dev": "node --watch src/index.js",
    "lint": "node --check src/index.js src/message-utils.js test/message-utils.test.js",
    "start": "node src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "telegraf": "^4.16.3"
  }
}
