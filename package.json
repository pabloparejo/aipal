{
  "name": "aipal",
  "version": "0.1.8",
  "private": true,
  "description": "Telegram bot that forwards messages to a local CLI agent (Codex by default).",
  "main": "src/index.js",
  "type": "commonjs",
  "scripts": {
    "build": "node --check src/index.js",
    "dev": "node --watch src/index.js",
    "lint": "git ls-files -z -- '*.js' | xargs -0 node --check",
    "start": "node src/index.js",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-cron": "^4.2.1",
    "telegraf": "^4.16.3"
  }
}
